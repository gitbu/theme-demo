const lighter = {
  '@text-color': 'black',
  '@bg-color': 'white',
};
const dark = {
  '@text-color': 'white',
  '@bg-color': 'black',
};
const red = {
  '@text-color': 'white',
  '@bg-color': 'red',
};

const theme = {
  lighter,
  red,
  dark,
};

export default theme;
